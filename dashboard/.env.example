# Port for the application (default is 3000)
PORT=3000

# Node environment (production or development)
NODE_ENV=production

# Any API endpoints your frontend might need (if applicable)
# NEXT_PUBLIC_API_URL=https://example.com/api

# Database connection string (if using a database)
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

# Public API URL for frontend requests
NEXT_PUBLIC_API_URL=https://api.example.com

# Secret key for encryption/session (generate a random string)
SECRET_KEY=your-secret-key-here

# Other application-specific variables
# Add any other variables your application needs 